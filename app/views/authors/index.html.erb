<h1>Authors</h1>
  <%= page_entries_info @authors %>
  <ol>
    <% @authors.each do |author| %>
      <li>
        <%= link_to(author.name, author) %><br/>
      </li>
    <% end %>
  </ol>

<%= paginate @authors %>