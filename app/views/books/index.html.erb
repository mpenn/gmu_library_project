<h1>Books</h1>
<p>Available as <%= formatted_time(@available_at) %></p>

  <ol>
    <% @books.each do |book| %>
      <li>
        <%= link_to(book.title, book) %>
        by <%= book.author_id %><br/>
      </li>
    <% end %>
  </ol>